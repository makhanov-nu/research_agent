"""Evaluation tools for research-agent subagent quality measurement.

Contains:
- judge.py   — batch LLM-judge that scores unlabeled completed tasks (1–5).
- harness.py — freeze a golden eval set and run model candidates against it.
"""
